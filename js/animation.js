"use strict";

// let parallax = () => {
// 	gsap.registerPlugin(ScrollTrigger);

// 	gsap.utils.toArray(".imageContainer").forEach(function (container) {
// 		let image = container.querySelector("img");

// 		let tl = gsap.timeline({
// 			scrollTrigger: {
// 				trigger: container,
// 				scrub: true,
// 				pin: false,
// 			},
// 		});
// 		tl.from(image, {
// 			y: "-200px",
// 			ease: "none",
// 		}).to(image, {
// 			y: "200px",
// 			ease: "none",
// 		});
// 	});
// };

// // Only calls when dom is ready
// $(document).ready(parallax);
